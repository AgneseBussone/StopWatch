<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>
        <item name="background_color">@color/background_color</item>
        <item name="background_color_blurr">@color/background_color_blurr</item>
        <item name="numbers_color">@color/black</item>
        <item name="settings_background">@color/colorPrimaryLight</item>
    </style>

    <style name="NightAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>
        <item name="background_color">@color/background_color_night</item>
        <item name="background_color_blurr">@color/background_color_blurr_night</item>
        <item name="numbers_color">@color/white</item>
        <item name="settings_background">@color/background_color_night</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="RadioButtonStyle" parent="@android:style/Widget.CompoundButton">
        <item name="android:background">@drawable/page_selector</item>
        <item name="android:button">@null</item>
    </style>

    <!-- The listView for laps need this to show the correct color for end-scrolling
         I don't know why it's not inherited by the AppTheme....-->
    <style name="MyEdgeOverlayTheme">
        <item name="android:colorEdgeEffect">@color/colorPrimary</item>
    </style>

</resources>
